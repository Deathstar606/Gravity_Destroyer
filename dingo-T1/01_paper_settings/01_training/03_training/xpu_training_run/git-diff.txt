diff --git a/ci/dingo-ci b/ci/dingo-ci
index 89c611ac..18a5866a 100755
--- a/ci/dingo-ci
+++ b/ci/dingo-ci
@@ -303,6 +303,15 @@ def check_gpus():
             print(f"  Device Name: {torch.cuda.get_device_name(i)}")
             props = torch.cuda.get_device_properties(i)
             print(f"  Memory: {props.total_memory / 1e9:.2f} GB")
+    elif hasattr(torch, "xpu") and torch.xpu.is_available():
+        print("XPU is available")
+        num_gpus = torch.xpu.device_count()
+        print(f"Found {num_gpus} XPU(s)")
+        for i in range(num_gpus):
+            print(f"\nXPU {i}:")
+            print(f"  Device Name: {torch.xpu.get_device_name(i)}")
+            props = torch.xpu.get_device_properties(i)
+            print(f"  Memory: {props.total_memory / 1e9:.2f} GB")
             print(f"  Compute Capability: {props.major}.{props.minor}")
     else:
         print("No CUDA-capable devices found")
diff --git a/dingo/core/nn/transformer.py b/dingo/core/nn/transformer.py
index eece9a8b..8a58f424 100644
--- a/dingo/core/nn/transformer.py
+++ b/dingo/core/nn/transformer.py
@@ -506,6 +506,93 @@ class TransformerModel(nn.Module):
             if p.dim() > 1:
                 nn.init.xavier_uniform_(p)
 
+    def freeze_all_except_last_n_layers(self, n: int = 2) -> None:
+        """
+        Freeze all transformer layers except the last n layers.
+        This is useful for transfer learning where you want to fine-tune only the last few layers.
+
+        Parameters
+        ----------
+        n : int
+            Number of layers from the end to keep unfrozen. Default: 2
+        """
+        total_layers = len(self.transformer_encoder.layers)
+        freeze_until = total_layers - n
+
+        # Freeze tokenizer
+        if self.tokenizer is not None:
+            for param in self.tokenizer.parameters():
+                param.requires_grad = False
+
+        # Freeze positional encoding
+        if self.positional_encoding is not None:
+            for param in self.positional_encoding.parameters():
+                param.requires_grad = False
+
+        # Freeze block encoding
+        if self.block_encoding is not None:
+            for param in self.block_encoding.parameters():
+                param.requires_grad = False
+
+        # Freeze transformer layers 0 to (freeze_until - 1)
+        for i, layer in enumerate(self.transformer_encoder.layers):
+            if i < freeze_until:
+                for param in layer.parameters():
+                    param.requires_grad = False
+            else:
+                for param in layer.parameters():
+                    param.requires_grad = True
+
+        # Freeze final net if it exists
+        if self.final_net is not None:
+            for param in self.final_net.parameters():
+                param.requires_grad = False
+
+    def unfreeze_all_layers(self) -> None:
+        """
+        Unfreeze all layers of the model to enable training on the entire network.
+        """
+        for param in self.parameters():
+            param.requires_grad = True
+
+    def get_frozen_status(self) -> dict:
+        """
+        Get a summary of which parameters are frozen and which are trainable.
+
+        Returns
+        -------
+        dict
+            Dictionary with counts of trainable and frozen parameters for each component.
+        """
+        status = {}
+
+        if self.tokenizer is not None:
+            trainable = sum(p.numel() for p in self.tokenizer.parameters() if p.requires_grad)
+            frozen = sum(p.numel() for p in self.tokenizer.parameters() if not p.requires_grad)
+            status["tokenizer"] = {"trainable": trainable, "frozen": frozen}
+
+        if self.positional_encoding is not None:
+            trainable = sum(p.numel() for p in self.positional_encoding.parameters() if p.requires_grad)
+            frozen = sum(p.numel() for p in self.positional_encoding.parameters() if not p.requires_grad)
+            status["positional_encoding"] = {"trainable": trainable, "frozen": frozen}
+
+        if self.block_encoding is not None:
+            trainable = sum(p.numel() for p in self.block_encoding.parameters() if p.requires_grad)
+            frozen = sum(p.numel() for p in self.block_encoding.parameters() if not p.requires_grad)
+            status["block_encoding"] = {"trainable": trainable, "frozen": frozen}
+
+        for i, layer in enumerate(self.transformer_encoder.layers):
+            trainable = sum(p.numel() for p in layer.parameters() if p.requires_grad)
+            frozen = sum(p.numel() for p in layer.parameters() if not p.requires_grad)
+            status[f"transformer_layer_{i}"] = {"trainable": trainable, "frozen": frozen}
+
+        if self.final_net is not None:
+            trainable = sum(p.numel() for p in self.final_net.parameters() if p.requires_grad)
+            frozen = sum(p.numel() for p in self.final_net.parameters() if not p.requires_grad)
+            status["final_net"] = {"trainable": trainable, "frozen": frozen}
+
+        return status
+
     def forward(
         self,
         x: Tensor,
@@ -529,15 +616,43 @@ class TransformerModel(nn.Module):
         output: Tensor
             shape [batch_size, d_out_of_final_net if final_net else d_model]
         """
+        # Force flush to guarantee we see the print before a C++ crash
+        print("\n--- ENTERING TRANSFORMER FORWARD ---", flush=True)
+        torch.xpu.synchronize() # Force Python to wait for the XPU
+
         if self.tokenizer is not None:
+            print(">> Executing Tokenizer...", flush=True)
             if self.tokenizer.context_features is not None:
                 x = self.tokenizer(x, context=position)
             else:
                 x = self.tokenizer(x)
+            torch.xpu.synchronize()
+            print(">> Tokenizer SAFE.", flush=True)
+        if self.positional_encoding is None:
+            print("you Niggered UP FAM 💀")
         if self.positional_encoding is not None:
+            print(">> Executing Positional Encoding...", flush=True)
+            
+            # Diagnostic Check on Positional Indices
+            pos_indices = position[..., 0:2].long()
+            print(f">> Positional Indices Min: {pos_indices.min().item()}, Max: {pos_indices.max().item()}", flush=True)
+            
             x = self.positional_encoding(x, position[..., 0:2])
+            torch.xpu.synchronize()
+            print(">> Positional Encoding SAFE.", flush=True)
+        if self.positional_encoding is None:
+            print("you Niggered UP FAM 🫷")
         if self.block_encoding is not None:
+            print(">> Executing Block Encoding...", flush=True)
+            
+            # Diagnostic Check on Block Indices
+            block_indices = position[..., 2].long()
+            print(f">> Block Indices Min: {block_indices.min().item()}, Max: {block_indices.max().item()}", flush=True)
+            
             x = self.block_encoding(x, position[..., 2])
+            torch.xpu.synchronize()
+            print(">> Block Encoding SAFE.", flush=True)
+            x = self.block_encoding(x, block_indices)
 
         if self.pooling == "cls":
             # Prepend the class token.
@@ -586,7 +701,7 @@ class TransformerModel(nn.Module):
         logging_info["num_all_tokens"] = (
             src_key_padding_mask.shape[0] * src_key_padding_mask.shape[1]
         )
-
+        print("🍎🍎🍎", logging_info, "finished 1 Epoch")
         return x, logging_info
 
 
diff --git a/dingo/core/posterior_models/base_model.py b/dingo/core/posterior_models/base_model.py
index 933cf8a7..e514e356 100644
--- a/dingo/core/posterior_models/base_model.py
+++ b/dingo/core/posterior_models/base_model.py
@@ -21,7 +21,7 @@ from typing import Callable, Optional
 import torch
 from torch.utils.data import Dataset
 import torch.distributed as dist
-from torch.cuda.amp import GradScaler, autocast
+from torch.amp import GradScaler, autocast
 
 import dingo.core.utils.trainutils
 import dingo.core.utils as utils
@@ -49,7 +49,7 @@ class BasePosteriorModel(ABC):
         metadata: dict = None,
         embedding_net_builder: Callable = None,
         initial_weights: dict = None,
-        device: str = "cuda",
+        device: str = None,
         load_training_info: bool = True,
     ):
         """
@@ -71,6 +71,14 @@ class BasePosteriorModel(ABC):
 
         self.version = f"dingo={get_version()}"  # dingo version
 
+        if device is None:
+            if torch.cuda.is_available():
+                device = "cuda"
+            elif hasattr(torch, "xpu") and torch.xpu.is_available():
+                device = "xpu"
+            else:
+                device = "cpu"
+
         self.device = None
         self.rank = None
         self.optimizer_kwargs = None
@@ -78,7 +86,7 @@ class BasePosteriorModel(ABC):
         self.scheduler_kwargs = None
         self.embedding_net_builder = embedding_net_builder
         self.initial_weights = initial_weights
-
+        
         self.metadata = metadata
         if self.metadata is not None:
             self.model_kwargs = self.metadata["train_settings"]["model"]
@@ -206,11 +214,20 @@ class BasePosteriorModel(ABC):
         """
         Put model to device, and set self.device accordingly.
         """
-        if "cpu" not in device and "cuda" not in device:
-            raise ValueError(f"Device should contain either cpu or cuda, got {device}.")
-        if ":" in device:
-            self.rank = int(device.split(":")[1])
-        self.device = torch.device(device)
+        # 1. Cast to string immediately to support both str and torch.device inputs safely
+        device_str = str(device)
+        
+        # 2. Validate the device type using the string representation
+        if "cpu" not in device_str and "cuda" not in device_str and "xpu" not in device_str:
+            raise ValueError(f"Device should contain either cpu, cuda, or xpu, got {device_str}.")
+        
+        # 3. Safely split the string to extract the rank identifier if present
+        if ":" in device_str:
+            self.rank = int(device_str.split(":")[1])
+            
+        # 4. Standardize self.device as a proper torch.device object
+        self.device = torch.device(device_str)
+        
         print(f"Putting posterior model to device {self.device}.")
         self.network.to(self.device)
 
@@ -330,7 +347,7 @@ class BasePosteriorModel(ABC):
         self,
         model_filename: str,
         load_training_info: bool = True,
-        device: str = "cuda",
+        device: str = "xpu",
     ):
         """
         Load a posterior model from the disk.
@@ -350,7 +367,7 @@ class BasePosteriorModel(ABC):
         # machine may have moved the model from 'cuda' to 'cpu'.
         ext = os.path.splitext(model_filename)[-1]
         if ext == ".pt":
-            d = torch.load(model_filename, map_location=device)
+            d = torch.load(model_filename, map_location="cpu")
         elif ext == ".hdf5":
             d = self._load_model_from_hdf5(model_filename)
         else:
@@ -629,7 +646,7 @@ def train_epoch(
     if automatic_mixed_precision:
         # Create scaler for automatic mixed precision (amp)
         # Warning: gradient clipping requires special treatment in amp
-        scaler = GradScaler()
+        scaler = GradScaler("xpu")
 
     for batch_idx, data in enumerate(dataloader):
         loss_info.update_timer("Dataloader")
@@ -638,7 +655,7 @@ def train_epoch(
         # Data to device
         data = [d.to(pm.device, non_blocking=True) for d in data]
         if automatic_mixed_precision:
-            with autocast():
+            with autocast(device_type="xpu", dtype=torch.bfloat16):
                 # Compute loss
                 loss, logging_info = pm.loss(data[0], *data[1:])
             # Backward pass, Note: Backward passes under autocast are not recommended
diff --git a/dingo/core/utils/backward_compatibility.py b/dingo/core/utils/backward_compatibility.py
index 0aacad50..43ecede9 100644
--- a/dingo/core/utils/backward_compatibility.py
+++ b/dingo/core/utils/backward_compatibility.py
@@ -24,6 +24,10 @@ def torch_available_devices() -> List[Device]:
     """
     devices: List[Device] = []
 
+    # xpu
+    if hasattr(torch, "xpu") and torch.xpu.is_available():
+        devices.append("xpu")
+
     # cuda
     if torch.cuda.is_available():
         devices.append("cuda")
diff --git a/dingo/core/utils/scheduler.py b/dingo/core/utils/scheduler.py
index 3ac14a3a..9765774f 100644
--- a/dingo/core/utils/scheduler.py
+++ b/dingo/core/utils/scheduler.py
@@ -11,7 +11,6 @@ from torch.optim.lr_scheduler import (
     ReduceLROnPlateau,
     StepLR,
     SequentialLR,
-    _check_verbose_deprecated_warning,
 )
 
 
@@ -62,7 +61,6 @@ class CustomSequentialLR(SequentialLR):
                 f"than the number of milestone points, but got number of schedulers {len(schedulers)} and the "
                 f"number of milestones to be equal to {len(milestones)}"
             )
-        _check_verbose_deprecated_warning(verbose)
         self._schedulers = schedulers
         self._milestones = milestones
         self.last_epoch = last_epoch + 1
diff --git a/dingo/core/utils/torchutils.py b/dingo/core/utils/torchutils.py
index d09749da..1bae6733 100644
--- a/dingo/core/utils/torchutils.py
+++ b/dingo/core/utils/torchutils.py
@@ -20,6 +20,14 @@ def get_cuda_info() -> dict[str, Any]:
 
     # No CUDA devices available
     if not torch.cuda.is_available():
+        if hasattr(torch, "xpu") and torch.xpu.is_available():
+            return {
+                "device count": torch.xpu.device_count(),
+                "device name": torch.xpu.get_device_name(0),
+                "memory (GB)": round(
+                    torch.xpu.get_device_properties(0).total_memory / 1024**3, 1
+                ),
+            }
         return {}
 
     # CUDA devices are available
@@ -352,6 +360,8 @@ def set_seed_based_on_rank(rank: int):
         torch.cuda.manual_seed(initial_torch_seed + rank)
         # Only use deterministic convolution algorithms
         torch.backends.cudnn.deterministic = True
+    elif hasattr(torch, "xpu") and torch.xpu.is_available():
+        torch.xpu.manual_seed(initial_torch_seed + rank)
 
     # Numpy expect a different seed range
     reduced_seed = int(initial_torch_seed) % (2**32 - 1)
@@ -363,15 +373,20 @@ def setup_ddp(rank: int, world_size: int):
     os.environ["MASTER_PORT"] = "12355"
 
     # initialize the process group
-    if dist.is_nccl_available():
+    if dist.is_nccl_available() and torch.cuda.is_available():
         dist.init_process_group(backend="nccl", rank=rank, world_size=world_size)
+    elif dist.is_ccl_available() or (hasattr(torch, "xpu") and torch.xpu.is_available()):
+        dist.init_process_group(backend="ccl", rank=rank, world_size=world_size)
     else:
         raise ValueError(
-            "Backends nccl not available for multi-GPU training with distributed data parallel."
+            "Backends nccl/ccl not available for multi-GPU training with distributed data parallel."
             "Go back to single-GPU training."
         )
     # Assign correct device to process
-    torch.cuda.set_device(rank)
+    if torch.cuda.is_available():
+        torch.cuda.set_device(rank)
+    elif hasattr(torch, "xpu") and torch.xpu.is_available():
+        torch.xpu.set_device(rank)
 
     print(
         f"Process group initialized with backend {dist.get_backend()}, rank {dist.get_rank()}, "
diff --git a/dingo/gw/training/train_pipeline.py b/dingo/gw/training/train_pipeline.py
index a047a29b..207bf026 100644
--- a/dingo/gw/training/train_pipeline.py
+++ b/dingo/gw/training/train_pipeline.py
@@ -614,6 +614,19 @@ def initialize_stage(
                 f"on {num_gpus} CPU/GPU(s) with {num_optimizer_steps} optimizer steps per epoch. "
             )
 
+        # Freeze all layers except the last 2 transformer blocks for XPU transfer learning
+        embedding_net = getattr(pm.network, 'embedding_net', pm.network)
+        if hasattr(embedding_net, 'freeze_all_except_last_n_layers'):
+            embedding_net.freeze_all_except_last_n_layers(n=2)
+            if print_output:
+                print("Froze all except last 2 layers of the embedding network.")
+        else:
+            if print_output:
+                print("Warning: freeze_all_except_last_n_layers method not found. Attempting manual freezing.")
+            for param in pm.network.parameters():
+                param.requires_grad = False
+            # Try to unfreeze last layers if it's a known architecture, else manual unfreezing is required.
+
         pm.initialize_optimizer_and_scheduler(num_optimizer_steps=num_optimizer_steps)
 
     # Freeze/unfreeze RB layer if necessary
diff --git a/misc_scripts/evaluation.py b/misc_scripts/evaluation.py
index 23da2772..6733a520 100644
--- a/misc_scripts/evaluation.py
+++ b/misc_scripts/evaluation.py
@@ -138,7 +138,7 @@ if __name__ == "__main__":
     base_dir = args.save_dir
 
     pm = build_model_from_kwargs(
-        filename=args.model_path, device="cuda" if torch.cuda.is_available() else "cpu"
+        filename=args.model_path, device="cuda" if torch.cuda.is_available() else "xpu"
     )
 
     evaluate_model(